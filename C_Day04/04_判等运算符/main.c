/*
&& || !
注意：
1.&&和||会发生短路现象，ex 0 && xxxx 其结果一定是 0，后面结果不会计算
e1 && e2,先计算e1表达式，若e1为false，则不会再计算e2，e2被短路
e1 || e2,先计算e1表达式，若e1为true，则不会再计算e2，e2被短路
好处：方便编程
*/
#include<stdio.h>
int main(void) {

	return 0;
}